// not code file 

const assert = require("assert");

describe(extendsOut, ()=>{
    it('the menu bar when hovers extends out', ()=>{
        // testing the file. If testing work we can use MOCHA
        assert.okay(3 + 7 === 9);
    
    });
});